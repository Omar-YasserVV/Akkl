{
  "$schema": "https://json.schemastore.org/tsconfig",
  "extends": "@repo/typescript-config/base.json",
  "include": ["src/**/*.ts", "*.config.ts"], // Ensure config files are included
  "exclude": ["node_modules", "dist"]
}
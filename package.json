{
  "name": "akkl-monorepo",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "generate": "turbo run generate",
    "clean": "rm -rf node_modules **/*/node_modules"
  },
  "devDependencies": {
    "turbo": "^2.7.4"
  }
}
